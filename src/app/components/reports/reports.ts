import { Component } from '@angular/core';

@Component({
  selector: 'app-transactions', // Mude para 'app-reports', 'app-settings', etc.
  standalone: true,
  template: '<h1>Página de Transações</h1>', // Mude o texto para cada página
})
export class TransactionsComponent {} // O nome da classe deve ser este e ter 'export'
